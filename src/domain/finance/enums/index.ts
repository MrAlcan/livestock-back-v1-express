export enum IDType {
  NIT = 'NIT',
  CI = 'CI',
  PASSPORT = 'PASSPORT',
  OTHER = 'OTHER',
}

export enum FinancialType {
  INCOME = 'INCOME',
  EXPENSE = 'EXPENSE',
}

export enum PaymentMethod {
  CASH = 'CASH',
  TRANSFER = 'TRANSFER',
  CHECK = 'CHECK',
  CARD = 'CARD',
  OTHER = 'OTHER',
}

export enum FinancialStatus {
  PENDING = 'PENDING',
  APPROVED = 'APPROVED',
  PAID = 'PAID',
  CANCELLED = 'CANCELLED',
}
